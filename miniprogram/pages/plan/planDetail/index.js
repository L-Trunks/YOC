//index.js
const app = getApp()
const db = wx.cloud.database({})
const _ = db.command
import { dateDiff, weekDay, dateTimeStamp, formatDateTime } from '../../../util/util'
Page({
  data: {

  },

  onLoad: function () {

  },
})
